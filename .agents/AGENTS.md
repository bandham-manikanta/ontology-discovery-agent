# Workspace Customization Rules

- **Coordinating Brain (Cursor/Agent CLI)**: The agent must act as the coordinating brain and orchestrator. For complex execution workloads (such as environment creation, dependency resolution, package compilation, database setup, and running servers), leverage the local CLI and command-line execution tools to perform the execution and troubleshoot packages, delegating execution steps while maintaining overall architectural control.
